• Unify fraction denominator settings and segmented controls.
• Maintain independent numerator and denominator state when switching slots.
• Remove active input cursor and use clean static contrast for active input.
• Support streak counter options and hide streak in arithmetic quiz mode.
